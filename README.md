# Система управления диалогами call-центра

<div align="center">

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![Django](https://img.shields.io/badge/Django-4.2.7-green.svg)

*Интеллектуальная система для обработки и управления диалогами в call-центрах с автоматической транскрипцией аудиозаписей*

</div>

## Описание проекта

**Ambity** — это веб-приложение на Django, предназначенное для управления диалогами в call-центрах. Система автоматически обрабатывает аудиозаписи телефонных разговоров, создает транскрипции с разделением по спикерам и предоставляет удобный интерфейс для работы с карточками контактов.

### Основные возможности

- **Автоматическая транскрипция** аудиозаписей с помощью AssemblyAI
- **Разделение по спикерам** (оператор/клиент)
- **Система статусов** для отслеживания обработки диалогов
- **Управление контактами** и операторами
- **Адаптивный интерфейс** с Bootstrap
- **Административная панель** Django

## Архитектура проекта

```
ambity/
├──  ambity/                    # Основные настройки Django
│   ├── settings.py              # Конфигурация проекта
│   ├── urls.py                  # Главные URL-маршруты
│   └── wsgi.py                  # WSGI конфигурация
├──  dialogs/                   # Приложение диалогов
│   ├── models.py                # Модели данных
│   ├── views.py                 # Представления
│   ├── urls.py                  # URL-маршруты
│   ├── admin.py                 # Настройки админ-панели
│   └── transcription.py         # Логика транскрипции
├──  templates/                # HTML шаблоны
│   ├── base.html                # Базовый шаблон
│   ├── dialogs/                 # Шаблоны диалогов
│   └── includes/                # Переиспользуемые компоненты
├── static/                   # Статические файлы
│   ├── css/                     # Стили CSS
│   ├── js/                      # JavaScript
│   └── img/                     # Изображения и favicon
└──  db.sqlite3                # База данных SQLite
```

##  Быстрый старт


### 1. Клонирование репозитория

```bash
git clone https://github.com/DevouringOG/Ambity_project.git
cd Ambity_project
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
cd ambity
pip install -r requirements/prod.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```env
# AssemblyAI API ключ для транскрипции
ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here

# Django настройки (опционально)
DEBUG=True
SECRET_KEY=your_secret_key_here
```

### 5. Инициализация базы данных

```bash
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser  # Создание админа (опционально)
```

### 6. Запуск

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/dialogs/`

## 📖 Использование

### Основные URL-маршруты:

- 🏠 **/** - Главная страница (список диалогов)
- 📋 **/dialogs/** - Список всех диалогов
- 👁️ **/dialogs/<id>/** - Детальная информация о диалоге
- ➕ **/dialogs/create_dialog/** - Создание нового диалога
- 🛡️ **/admin/** - Административная панель

### Создание нового диалога:

1. Перейдите на `/dialogs/create_dialog/`
2. Система автоматически:
   - Получит данные с внешнего API
   - Обработает аудиозапись
   - Создаст транскрипцию
   - Сохранит диалог в базе данных
   - Перенаправит на страницу диалога

## 🔧 Технологический стек